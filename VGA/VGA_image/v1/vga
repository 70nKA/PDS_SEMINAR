module VGA_image
    (
        input color_in,
        output i_VGA_RED,
        output i_VGA_GREEN,
        output i_VGA_BLUE
    );

    assign i_VGA_RED = (color_in == 1)? 1 : 0;
    assign i_VGA_GREEN = (color_in == 1)? 1 : 0;
    assign i_VGA_BLUE = (color_in == 1)? 1 : 0; 
endmodule